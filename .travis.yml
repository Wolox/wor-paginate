language: ruby
rvm:
  - 2.2.2
  - 2.3.3
  - 2.4.0

install:
  - gem install bundler
  - bundle install --retry=3

script:
  - bundle exec rubocop spec lib -R --format simple
  - bundle exec rspec
  - bundle exec codeclimate-test-reporter
